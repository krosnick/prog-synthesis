function add(numA:number, numB:number) {
   return numA + numB;
}
const num2:number = 2;

// ---- Do not edit above this line -----
const leg1:number = 3;
const leg2:number = 4;

const st:number = Math.pow(num2, leg2);
const n:number = Math.pow(leg1, num2);

const tf:number = add(st, n);
